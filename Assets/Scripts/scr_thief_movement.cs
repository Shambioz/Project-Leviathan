using JetBrains.Annotations;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scr_thief_movement : MonoBehaviour
{
    /*public ThiefMovement scr_thief_spawning;
    public float speed = 5f;
    public GameObject target_object;
    private Vector3 target;
    // Start is called before the first frame update
    void Start()
    {
        target = target_object.transform.position;
    }

    // Update is called once per frame
    void Update()
    {
       if(scr_thief_spawning != null)
        {

        }
        scr_thief_spawning 
        enemy.transform.position = Vector3.MoveTowards(transform.position, target, speed * Time.deltaTime);
    }*/
}
